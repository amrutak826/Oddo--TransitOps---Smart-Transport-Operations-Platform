<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_transit_maintenance_list" model="ir.ui.view">
        <field name="name">transit.maintenance.list</field>
        <field name="model">transit.maintenance</field>
        <field name="arch" type="xml">
            <list string="Maintenance Records">
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="maintenance_type"/>
                <field name="scheduled_date"/>
                <field name="next_service_date"/>
                <field name="cost"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_transit_maintenance_form" model="ir.ui.view">
        <field name="name">transit.maintenance.form</field>
        <field name="model">transit.maintenance</field>
        <field name="arch" type="xml">
            <form string="Maintenance">
                <header>
                    <button name="action_start" type="object" string="Start" class="btn-primary" invisible="state != 'scheduled'"/>
                    <button name="action_complete" type="object" string="Mark Done" class="btn-primary" invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar" statusbar_visible="scheduled,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="vehicle_id"/>
                            <field name="maintenance_type"/>
                            <field name="cost"/>
                        </group>
                        <group>
                            <field name="scheduled_date"/>
                            <field name="next_service_date"/>
                            <field name="completed_date" readonly="1"/>
                        </group>
                    </group>
                    <group string="Description">
                        <field name="description" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_transit_maintenance" model="ir.actions.act_window">
        <field name="name">Maintenance</field>
        <field name="res_model">transit.maintenance</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Schedule your first maintenance record.</p>
        </field>
    </record>

</odoo>
